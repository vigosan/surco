appId: com.vigosan.rotulo
productName: Rótulo
directories:
  buildResources: build
  output: dist
files:
  - out/**
  - package.json
mac:
  category: public.app-category.music
  target:
    - dmg
  artifactName: ${productName}-${version}.${ext}
